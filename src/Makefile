# comments begin with #

# for convenience, define variable $(OBJS) to list of object files

OBJS = uimain.o tokenizer.o

# make has a "default rule" specifying how to build an "object" file (.o) form a C source file (.c)
# this rule includes $(CFLAGS) as one of cc's parameters

# CFLAGS = g -03 # -g for debug, -03 for optimization#

# first target is built by default. Usually namae "all"
all: tokenize

# tokenize (target) depends on all object files (prerequisites)
tokenize: $(OBJS)
	cc -o tokenize $(CFLAGS) $(OBJS)

# all object files built from c files that include header files

#deletes files generated by compilation
clean:
	rm -f *.o tokenize

run:tokenize
	./tokenize
